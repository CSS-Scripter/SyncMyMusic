module.exports = {
  'Should start a rehearsal': browser => {
    browser.init()
      .openHomepage()
      .waitForElementVisible('Header', 10000)
      .click('a[href="/login"]')
      .waitForElementVisible('#login-form', 10000)
      // .setValue('#email', "richardkerkvliet@hotmail.com")
      // .setValue('#password', "admin")
      .setValue('#email', "test@email.nl")
      .setValue('#password', "s3m1eybkArcvEIjdZyWI")
      .click('button[type="submit"]')
      .waitForElementVisible('.left-content', 10000)
      .click('.left-content button')
      .waitForElementVisible('.band', 10000)
      .click('#start-jamming')
      .waitForElementVisible('#rehearsal', 10000)
      .assert.containsText('#welcome', "Welkom bij de repetitie")
  },

  'Should select first song, show not-available message': browser => {
    browser.init()
      .openHomepage()
      .waitForElementVisible('Header', 10000)
      .click('a[href="/login"]')
      .waitForElementVisible('#login-form', 10000)
      // .setValue('#email', "richardkerkvliet@hotmail.com")
      // .setValue('#password', "admin")
      .setValue('#email', "test@email.nl")
      .setValue('#password', "s3m1eybkArcvEIjdZyWI")
      .click('button[type="submit"]')
      .waitForElementVisible('.left-content', 10000)
      .click('.left-content button')
      .waitForElementVisible('.band', 10000)
      .click('#start-jamming')
      .waitForElementVisible('#rehearsal', 10000)
      .pause(10000)
      .click('input[placeholder="Select song"]')
      .moveToElement('.site', 150, 315)
      .mouseButtonClick(0)
      .waitForElementVisible('#not-available', 10000)
  },

  'Should select different instrument, show pdf': browser => {
    browser.init()
      .openHomepage()
      .waitForElementVisible('Header', 10000)
      .click('a[href="/login"]')
      .waitForElementVisible('#login-form', 10000)
      // .setValue('#email', "richardkerkvliet@hotmail.com")
      // .setValue('#password', "admin")
      .setValue('#email', "test@email.nl")
      .setValue('#password', "s3m1eybkArcvEIjdZyWI")
      .click('button[type="submit"]')
      .waitForElementVisible('.left-content', 10000)
      .click('.left-content button')
      .waitForElementVisible('.band', 10000)
      .click('#start-jamming')
      .waitForElementVisible('#rehearsal', 10000)
      .pause(10000)
      .click('input[placeholder="Select song"]')
      .moveToElement('.site', 150, 315)
      .mouseButtonClick(0)
      .pause(1000)
      .waitForElementVisible('#not-available', 10000)
      .click('input[placeholder="Select option"]')
      .moveToElement('.site', 170, 300)
      .mouseButtonClick(0)
      .waitForElementVisible('#pdf', 10000)
  },
}